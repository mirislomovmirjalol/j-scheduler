// Test group for now — swap NEXT_PUBLIC_GROUP_URL in .env.local when the
// real club group's invite link is ready. Public value (not a secret), so
// NEXT_PUBLIC_ is correct here.
export const GROUP_URL = process.env.NEXT_PUBLIC_GROUP_URL!
